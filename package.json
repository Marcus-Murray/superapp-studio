{
  "name": "superapp-studio",
  "private": true,
  "version": "0.1.0",
  "description": "Tablet-first superapp blending Figma-like ideation, Notion-like docs, and AI code generation.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "lint": "echo \"No lint yet\"",
    "format": "echo \"No format yet\"",
    "test": "echo \"No tests at root; run tests per workspace\""
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
